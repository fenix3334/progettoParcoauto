document.addEventListener('DOMContentLoaded', ()=>{if(document.getElementById('kmChart')){const ctx=document.getElementById('kmChart').getContext('2d');const labels=JSON.parse('{{ labels|tojson|safe }}');const values=JSON.parse('{{ values|tojson|safe }}');new Chart(ctx,{type:'bar',data:{labels:labels,datasets:[{label:'Veicoli per Anno',data:values,backgroundColor:'rgba(0,123,255,0.5)'}]},options:{responsive:true,scales:{y:{beginAtZero:true}}}});}});